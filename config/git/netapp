[user]
	email         = Aaron.Schrab@netapp.com

[remote "origin"]
	fetch = +refs/pull-requests/*/from:refs/remotes/origin/pr/*

[credential]
	# Ignore any previously configured helpers
	helper =
	helper = store

# vim: ft=gitconfig tabstop=4 shiftwidth=4 noexpandtab
