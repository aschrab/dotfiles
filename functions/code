#autoload

function code {
  local TMUX=
  local PATH="/Applications/Visual Studio Code.app/Contents/Resources/app/bin:$PATH"
  if [ -x /usr/share/codium/bin/codium ] ; then
    /usr/share/codium/bin/codium "$@"
  else
    command code "$@"
  fi
}
