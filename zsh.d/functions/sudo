#autoload sudo

local args

if [[ $# -eq 2 ]] && [[ "$1" = '-u' ]]; then
  args=($*)
  set --
fi

if [[ $# -eq 0 ]]; then
  set HOME="$HOME" "$SHELL" -l
fi

command sudo $args SSH_AUTH_SOCK="$SSH_AUTH_SOCK" TMUX="$TMUX" "$@"
