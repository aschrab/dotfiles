#autoload

resolve() {
  local res cmd
  for cmd in "$@"; do
    res="$(which $cmd)"
    echo "$res"
    case "$res" in
      (*:*aliased*)
        resolve "${res##*: aliased to }"
        ;;
      (*)
        [[ "$res" = /* ]] || res=$commands[$res]
        if [[ -L "$res" ]]; then
          resolve $(readlink $res)
        fi
    esac
  done
}

resolve "$@"
